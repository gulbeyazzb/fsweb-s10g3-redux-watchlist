export const NEXT_SİRA = "plus 1 to 'sira' ";
export const PREV_SİRA = "plus 1 to 'sira' ";

export const nextSiraAction = () => {
  return { type: NEXT_SİRA };
};

export const prevSiraAction = () => {
  return { type: PREV_SİRA };
};
